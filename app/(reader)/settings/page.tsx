import ReaderPlaceholderPage from "@/components/main/ReaderPlaceholderPage";

export default function SettingsPage() {
  return (
    <ReaderPlaceholderPage
      eyebrow="Setting"
      title="Control the app the way you like it."
      description="This screen can hold theme options, language, notifications, privacy preferences, and all other reader settings while still using the same shared navigation."
    />
  );
}
